/* Reads saved reports only. No collector, session or external portal access. */
const performanceState={date:'',period:'daily',brand:'',branch:'',search:'',view:'sales',sortKey:'branch',sortDirection:'asc',cache:null,cacheKey:null,checkedAt:0,generation:0};
const performanceViews={
 sales:[['Successful Orders','Orders'],['Gross Sales','Gross sales · AED'],['Cancelled Orders','Cancelled']],
 operations:[['Customer Complaint rate','Complaints · %'],['Avoidable cancellation rate','Avoidable cancellation · %'],['Unavailable Time Duration Rate','Offline · %'],['Average preparation time (minutes)','Prep · min'],['Total AWT Duration (Minutes)','Total avoidable wait · min']],
 funnel:[['Impressions','Impressions'],['Viewed your menu','Viewed menu'],['Added items to cart','Added to cart'],['Placed an order','Placed order']]
};
function performanceNumber(v){if(v===null||v===undefined||typeof v!=='string'||!/^[-+]?\d+(?:\.\d+)?$/.test(v.trim()))return null;const n=Number(v);return Number.isFinite(n)?n:null;}
function performanceTotal(rows,key){const values=rows.filter(r=>r.present).map(r=>performanceNumber(r.values[key]));const usable=values.filter(v=>v!==null);return {value:usable.length?usable.reduce((a,b)=>a+b,0):null,populated:usable.length,total:values.length};}
function performanceFormat(v){const n=performanceNumber(v);return n===null?(v===null||v===undefined||v===''?'—':escapeHtml(v)):new Intl.NumberFormat('en-US',{maximumFractionDigits:2}).format(n);}
function performanceRows(data){return data.rows.map(r=>({...r,...storeIdentity({storeName:r.storeName||'Unnamed branch'})}));}
function performanceFiltered(data){
 const p=performanceState,rows=performanceRows(data).filter(r=>(!p.brand||r.brand===p.brand)&&(!p.branch||r.storeId===p.branch)&&(!p.search||r.displayName.toLowerCase().includes(p.search.toLowerCase())));
 return rows.sort((a,b)=>{
  if(p.sortKey==='branch')return (p.sortDirection==='asc'?1:-1)*(a.displayName.localeCompare(b.displayName)||a.storeId.localeCompare(b.storeId));
  const av=performanceNumber(a.values[p.sortKey]),bv=performanceNumber(b.values[p.sortKey]);
  if(av===null&&bv!==null)return 1;if(av!==null&&bv===null)return -1;
  if(av!==bv)return (p.sortDirection==='asc'?1:-1)*((av??0)-(bv??0));
  return a.displayName.localeCompare(b.displayName);
 });
}
function performanceSortHeader(label,key){const p=performanceState,active=p.sortKey===key,arrow=active?(p.sortDirection==='asc'?'▲':'▼'):'↕';return `<button class="sort-button ${active?'active':''}" data-perf-sort="${escapeHtml(key)}" aria-label="Sort ${escapeHtml(label)} ${active&&p.sortDirection==='asc'?'descending':'ascending'}">${escapeHtml(label)} <span aria-hidden="true">${arrow}</span></button>`;}
const performanceTierMeta=value=>({NONE:['Poor','poor',0],STARTER:['Fair','fair',1],ADVANCED:['Good','good',2],EXPERT:['Exceptional','exceptional',3],INELIGIBLE__LOW_ORDERS:['Not eligible','ineligible',-1]}[value]||['Not eligible','ineligible',-1]);
function projectedTier(row){
 const values=[row.rating,row.metrics?.failRate,row.metrics?.offlineRate,row.metrics?.inaccurateOrders,row.metrics?.avoidableWaitingTime];if(values.some(v=>v==null||!Number.isFinite(Number(v))))return ['Unknown','ineligible',null];
 const [rating,cancellation,offline,contacts,wait]=values.map(Number),grade=Math.min(
  rating>4.3?3:rating>4?2:rating>3.5?1:0,
  cancellation<0.5?3:cancellation<1?2:cancellation<2?1:0,
  offline<5?3:offline<10?2:offline<20?1:0,
  contacts<0.5?3:contacts<1?2:contacts<2?1:0,
  wait<3?3:wait<4?2:wait<8?1:0
 );
 return [['Poor','poor',0],['Fair','fair',1],['Good','good',2],['Exceptional','exceptional',3]][grade];
}
function performanceCoverage(rows){const keys=performanceViews.funnel.map(x=>x[0]);const present=rows.filter(r=>r.present);if(!present.length)return 'No report rows in this selection';const cells=present.flatMap(r=>keys.map(k=>performanceNumber(r.values[k])));const filled=cells.filter(v=>v!==null).length;return filled===0?'Funnel data unavailable from Talabat':filled===cells.length?'Funnel fields populated':'Funnel data partially available';}
async function renderPerformance(force=false){
 const p=performanceState,key=p.period==='daily'?'daily:'+p.date:p.period;const generation=++p.generation;
 if(force||!p.cache||p.cacheKey!==key||Date.now()-p.checkedAt>300000){
  main.innerHTML='<section class="loading-card">Loading saved Performance report…</section>';
  try{let data;if(p.period==='monthly')data={tstar:await api('/api/dashboard/performance/latest?dataset=tstar')};else data=await api('/api/dashboard/performance/latest'+(p.period==='weekly'?'?period=7d':p.date?'?date='+encodeURIComponent(p.date):''));if(generation!==p.generation||state.tab!=='performance')return;p.cache=data;p.cacheKey=key;p.checkedAt=Date.now();}
  catch{if(generation!==p.generation||state.tab!=='performance')return;main.innerHTML='<section class="error-card"><h2>Performance unavailable</h2><p>The saved report could not be read. Ratings remain available in their tabs.</p><button class="tab active" id="performanceRetry">Retry</button></section>';document.getElementById('performanceRetry').onclick=()=>renderPerformance(true);refreshState.querySelector('span:last-child').textContent='Performance unavailable';refreshState.querySelector('.live-dot').dataset.health='ERROR';return;}
 }
 if(state.tab!=='performance')return;
 if(p.period==='monthly')paintTstarMonthly();else paintPerformance();
}
function performancePeriodTabs(){const p=performanceState;return `<nav class="period-tabs" aria-label="Performance period">${[['daily','Daily'],['weekly','Last 7 days'],['monthly','Month to date']].map(([value,label])=>`<button class="tab ${p.period===value?'active':''}" data-performance-period="${value}">${label}</button>`).join('')}</nav>`;}
function attachPerformancePeriodTabs(){for(const button of main.querySelectorAll('[data-performance-period]'))button.onclick=()=>{performanceState.period=button.dataset.performancePeriod;performanceState.date='';performanceState.cache=null;renderPerformance();};}
function paintPerformance(){
 const p=performanceState,data=p.cache,all=performanceRows(data),rows=performanceFiltered(data);
 const brands=[...new Set(all.map(r=>r.brand))].sort();const branches=all.filter(r=>!p.brand||r.brand===p.brand).sort((a,b)=>a.displayName.localeCompare(b.displayName));
 const dates=[...new Set([...data.availableDates,...(p.date?[p.date]:[])])].sort().reverse();
 const opt=(v,label,selected)=>`<option value="${escapeHtml(v)}" ${v===selected?'selected':''}>${escapeHtml(label)}</option>`;
 const cards=[['Gross Sales','Gross sales','AED'],['Successful Orders','Successful orders',''],['Cancelled Orders','Cancelled orders',''],['Total customer complaints received','Customer complaints','']];
 const coverage=performanceCoverage(rows),missing=rows.filter(r=>!r.present).length;
 main.innerHTML=`<section class="page-heading"><div><div class="perf-eyebrow">TALABAT · ${p.period==='weekly'?'7-DAY':'DAILY'} PERFORMANCE</div><h2>Performance</h2><p>${p.period==='weekly'?`Period: <strong>${escapeHtml(data.periodFrom)} – ${escapeHtml(data.periodTo)}</strong> · ${data.dayCount} saved days`:`Report date: <strong>${escapeHtml(data.reportDate||'No saved report')}</strong>`} · Dubai calendar</p></div><button class="tab active" id="performanceRefresh">Refresh saved data</button></section>
 ${performancePeriodTabs()}
 <section class="perf-context"><span>Daily collection: 09:30 Dubai · two-day reporting lag</span><span>Saved: ${data.receivedAt?escapeHtml(new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Dubai',dateStyle:'medium',timeStyle:'short'}).format(new Date(data.receivedAt))):'No data'} Dubai</span></section>
 ${!p.date&&data.reportDate&&data.reportDate<data.expectedDate?`<p class="health-warning" role="status">Older report displayed. Expected reporting date: ${escapeHtml(data.expectedDate)}.</p>`:''}
 <section class="perf-controls" aria-label="Performance filters">
 ${p.period==='daily'?`<label>Report date<select class="select" id="performanceDate">${opt('','Latest available (two-day lag)',p.date)}${dates.map(d=>opt(d,d,p.date)).join('')}</select></label>`:''}
 <label>Brand<select class="select" id="performanceBrand">${opt('','All brands',p.brand)}${brands.map(b=>opt(b,b,p.brand)).join('')}</select></label>
 <label>Branch<select class="select" id="performanceBranch">${opt('','All branches',p.branch)}${branches.map(b=>opt(b.storeId,b.displayName,p.branch)).join('')}</select></label>
 <label>Search<input class="input" type="search" id="performanceSearch" value="${escapeHtml(p.search)}" placeholder="Brand or branch"></label>
 <label class="perf-sort-control">Sort<select class="select" id="performanceSort">${[['branch','Branch name'],...performanceViews[p.view]].flatMap(([key,label])=>[['asc','lowest first'],['desc','highest first']].map(([direction,suffix])=>opt(key+'|'+direction,label+' · '+(key==='branch'?(direction==='asc'?'A to Z':'Z to A'):suffix),p.sortKey+'|'+p.sortDirection))).join('')}</select></label></section>
 ${data.state==='EMPTY'?'<section class="loading-card">No saved report for this date. Choose another available date.</section>':`
 <section class="kpi-grid perf-kpis">${cards.map(([key,label,unit])=>{const t=performanceTotal(rows,key);return `<article class="kpi-card"><span class="kpi-label">${label}</span><strong class="kpi-value">${t.value===null?'—':new Intl.NumberFormat('en-US',{maximumFractionDigits:2}).format(t.value)}</strong><p class="perf-card-note">${unit} · ${t.populated}/${t.total} report rows${t.populated<t.total?' · partial':''}</p></article>`;}).join('')}</section>
 <div class="perf-quality"><span>${rows.filter(r=>r.present).length} reported / ${rows.length} scoped branches${missing?` · ${missing} absent from CSV`:''}</span><span>${coverage}</span></div>
 <p class="perf-note">Totals cover the filtered report rows. Missing values are shown as —, never zero. Rates and average times are shown per branch.</p>
 <nav class="perf-view-tabs" aria-label="Metric group">${Object.keys(performanceViews).map(v=>`<button class="tab ${p.view===v?'active':''}" data-perf-view="${v}" aria-pressed="${p.view===v}">${v==='sales'?'Sales & orders':v==='operations'?'Operations':'Customer funnel'}</button>`).join('')}</nav>
 <div class="table-wrap perf-table-wrap"><table class="compact-table perf-table"><thead><tr><th>${performanceSortHeader('Brand / branch','branch')}</th>${performanceViews[p.view].map(c=>`<th>${performanceSortHeader(c[1],c[0])}</th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td data-label="Branch"><button class="perf-branch" data-perf-id="${escapeHtml(r.storeId)}">${escapeHtml(r.displayName)}</button>${!r.present?'<span class="carried-forward">Not included in source CSV</span>':''}</td>${performanceViews[p.view].map(c=>`<td data-label="${c[1]}">${performanceFormat(r.values[c[0]])}</td>`).join('')}</tr>`).join(''):`<tr><td colspan="${performanceViews[p.view].length+1}" class="empty">No branches match these filters.</td></tr>`}</tbody></table></div>`}`;
 if(document.getElementById('performanceDate'))document.getElementById('performanceDate').onchange=e=>{p.date=e.target.value;renderPerformance();};
 document.getElementById('performanceBrand').onchange=e=>{p.brand=e.target.value;p.branch='';paintPerformance();};
 document.getElementById('performanceBranch').onchange=e=>{p.branch=e.target.value;paintPerformance();};
 document.getElementById('performanceSearch').oninput=e=>{const input=e.target,value=input.value,pos=input.selectionStart;p.search=value;paintPerformance();const replacement=document.getElementById('performanceSearch');replacement.focus();replacement.setSelectionRange(pos,pos);};
 document.getElementById('performanceSort').onchange=e=>{[p.sortKey,p.sortDirection]=e.target.value.split('|');paintPerformance();};
 document.getElementById('performanceRefresh').onclick=()=>renderPerformance(true);
 attachPerformancePeriodTabs();
 for(const b of main.querySelectorAll('[data-perf-view]'))b.onclick=()=>{p.view=b.dataset.perfView;if(p.sortKey!=='branch'&&!performanceViews[p.view].some(c=>c[0]===p.sortKey)){p.sortKey='branch';p.sortDirection='asc';}paintPerformance();};
 for(const b of main.querySelectorAll('[data-perf-sort]'))b.onclick=()=>{const key=b.dataset.perfSort;if(p.sortKey===key)p.sortDirection=p.sortDirection==='asc'?'desc':'asc';else{p.sortKey=key;p.sortDirection=key==='branch'?'asc':'desc';}paintPerformance();};
 for(const b of main.querySelectorAll('[data-perf-id]'))b.onclick=()=>performanceDetail(b.dataset.perfId);
 refreshState.querySelector('span:last-child').textContent='Saved reports · checked every 5 min';refreshState.querySelector('.live-dot').dataset.health=data.state==='SUCCESS'?'HEALTHY':'UNKNOWN';
}
function paintTstarMonthly(){
 const p=performanceState,t=p.cache.tstar,ratings=new Map((state.cloudResponse?.ratings||[]).filter(r=>r.platform==='talabat').map(r=>[String(r.storeIdentityKey||'').replace(/^TB_AE;/,''),r.rating]));
 const monthlySortKeys=new Set(['branch','tier','projectedTier','rating','avoidableWaitingTime','inaccurateOrders','offlineRate','failRate']);if(!monthlySortKeys.has(p.sortKey)){p.sortKey='branch';p.sortDirection='asc';}
 const rows=(t.rows||[]).map(r=>{const row={...r,...storeIdentity({storeName:r.storeName||'Unnamed branch'}),rating:ratings.get(r.storeId)??null};return {...row,projectedTier:projectedTier(row)};}).filter(r=>(!p.brand||r.brand===p.brand)&&(!p.branch||r.storeId===p.branch)&&(!p.search||r.displayName.toLowerCase().includes(p.search.toLowerCase())));
 const brands=[...new Set((t.rows||[]).map(r=>storeIdentity({storeName:r.storeName||'Unnamed branch'}).brand))].sort(),opt=(v,label,selected)=>`<option value="${escapeHtml(v)}" ${v===selected?'selected':''}>${escapeHtml(label)}</option>`;
 const metric=[['rating','Live rating'],['avoidableWaitingTime','Avg avoidable wait · min/order'],['inaccurateOrders','Customer contacts · %'],['offlineRate','Offline rate · %'],['failRate','Cancellation rate · %']];
 rows.sort((a,b)=>{if(p.sortKey==='branch')return(p.sortDirection==='asc'?1:-1)*a.displayName.localeCompare(b.displayName);if(p.sortKey==='tier'||p.sortKey==='projectedTier'){const av=p.sortKey==='tier'?performanceTierMeta(a.tier)[2]:a.projectedTier[2],bv=p.sortKey==='tier'?performanceTierMeta(b.tier)[2]:b.projectedTier[2];return(p.sortDirection==='asc'?1:-1)*((av??-2)-(bv??-2))||a.displayName.localeCompare(b.displayName);}const av=p.sortKey==='rating'?a.rating:a.metrics[p.sortKey],bv=p.sortKey==='rating'?b.rating:b.metrics[p.sortKey];if(av==null&&bv!=null)return 1;if(av!=null&&bv==null)return-1;return(p.sortDirection==='asc'?1:-1)*((av??0)-(bv??0))||a.displayName.localeCompare(b.displayName);});
 const periodFrom=t.rows?.find(r=>r.periodFrom)?.periodFrom||'—',periodTo=t.rows?.find(r=>r.periodTo)?.periodTo||'—';
 main.innerHTML=`<section class="page-heading monthly-heading"><div><div class="perf-eyebrow">TALABAT · T‑STAR</div><h2>Period performance</h2><p>Source period: <strong>${escapeHtml(periodFrom)} – ${escapeHtml(periodTo)}</strong></p></div><button class="tab active" id="performanceRefresh">Refresh saved data</button></section>${performancePeriodTabs()}<section class="monthly-explainer"><strong>How to read this view</strong><span>Current tier is fixed for this T‑Star period. Projected tier shows next period based on the tracked targets: rating, cancellations, offline time, customer contacts and avoidable wait.</span><span>Menu score, fair pricing and order eligibility are excluded, so the projection is directional—not a guaranteed Talabat tier. MTD orders remain hidden until the first complete month.</span></section><section class="monthly-targets" aria-label="Projected tier thresholds"><article class="target-fair"><strong>Fair</strong><span>Cancel/contact &lt;2% · Offline &lt;20% · Rating &gt;3.5 · Wait &lt;8m</span></article><article class="target-good"><strong>Good</strong><span>Cancel/contact &lt;1% · Offline &lt;10% · Rating &gt;4.0 · Wait &lt;4m</span></article><article class="target-exceptional"><strong>Exceptional</strong><span>Cancel/contact &lt;0.5% · Offline &lt;5% · Rating &gt;4.3 · Wait &lt;3m</span></article></section><section class="perf-controls monthly-controls"><label>Brand<select class="select" id="performanceBrand">${opt('','All brands',p.brand)}${brands.map(b=>opt(b,b,p.brand)).join('')}</select></label><label>Search<input class="input" type="search" id="performanceSearch" value="${escapeHtml(p.search)}" placeholder="Brand or branch"></label><label class="perf-sort-control">Sort<select class="select" id="performanceSort">${[['branch','Branch name'],['tier','Current tier'],['projectedTier','Projected tier'],...metric].flatMap(([key,label])=>[['asc','lowest first'],['desc','highest first']].map(([direction,suffix])=>opt(key+'|'+direction,label+' · '+(key==='branch'?(direction==='asc'?'A to Z':'Z to A'):suffix),p.sortKey+'|'+p.sortDirection))).join('')}</select></label></section><div class="perf-quality monthly-quality"><span>${t.activeCount} active / ${t.scopeCount} scoped branches</span><span>Rates are percentages · wait is average minutes per order</span></div><div class="table-wrap perf-table-wrap monthly-table-wrap"><table class="compact-table perf-table monthly-table"><colgroup><col class="branch-col"><col class="tier-col"><col class="tier-col">${metric.map(()=>'<col class="metric-col">').join('')}</colgroup><thead><tr><th>${performanceSortHeader('Brand / branch','branch')}</th><th>${performanceSortHeader('Current tier','tier')}</th><th>${performanceSortHeader('Projected tier','projectedTier')}</th>${metric.map(c=>`<th>${performanceSortHeader(c[1],c[0])}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>{const [tierLabel,tierClass]=performanceTierMeta(r.tier),[projectedLabel,projectedClass]=r.projectedTier;return `<tr><td data-label="Branch">${escapeHtml(r.displayName)}</td><td data-label="Current tier"><span class="tier-badge tier-${tierClass}">${tierLabel}</span></td><td data-label="Projected tier"><span class="tier-badge tier-${projectedClass}">${projectedLabel}</span></td><td data-label="Live rating">${r.rating==null?'—':Number(r.rating).toFixed(1)}</td><td data-label="Avg wait · min/order">${r.metrics.avoidableWaitingTime==null?'—':performanceFormat(String(r.metrics.avoidableWaitingTime))}</td><td data-label="Customer contacts · %">${r.metrics.inaccurateOrders==null?'—':performanceFormat(String(r.metrics.inaccurateOrders))+'%'}</td><td data-label="Offline rate · %">${r.metrics.offlineRate==null?'—':performanceFormat(String(r.metrics.offlineRate))+'%'}</td><td data-label="Cancellation rate · %">${r.metrics.failRate==null?'—':performanceFormat(String(r.metrics.failRate))+'%'}</td></tr>`;}).join('')}</tbody></table></div>`;
 document.getElementById('performanceBrand').onchange=e=>{p.brand=e.target.value;paintTstarMonthly();};document.getElementById('performanceSearch').oninput=e=>{p.search=e.target.value;paintTstarMonthly();};document.getElementById('performanceSort').onchange=e=>{[p.sortKey,p.sortDirection]=e.target.value.split('|');paintTstarMonthly();};document.getElementById('performanceRefresh').onclick=()=>renderPerformance(true);attachPerformancePeriodTabs();for(const b of main.querySelectorAll('[data-perf-sort]'))b.onclick=()=>{const key=b.dataset.perfSort;if(p.sortKey===key)p.sortDirection=p.sortDirection==='asc'?'desc':'asc';else{p.sortKey=key;p.sortDirection=key==='branch'?'asc':'desc';}paintTstarMonthly();};
}
function performanceDetail(id){
 const data=performanceState.cache,row=performanceRows(data).find(r=>r.storeId===id);if(!row)return;
 selectedStore=null;++detailRequest;chartData=[];
 detailContent.innerHTML=`<p class="perf-eyebrow">TALABAT · ${escapeHtml(data.reportDate)}</p><h2 id="detailTitle">${escapeHtml(row.displayName)}</h2><p class="perf-note">${row.present?'Original fields from the saved Performance CSV.':'This branch was in the report scope but had no CSV row. Its values are unknown.'}</p><div class="status-filters" aria-label="Performance history range">${[[7,'7 days'],[30,'30 days'],[90,'90 days']].map(([days,label])=>`<button class="filter-chip ${days===30?'active':''}" data-performance-days="${days}">${label}</button>`).join('')}</div><section id="performanceHistory"><div class="loading-card">Loading sales and order history…</div></section><dl class="perf-details">${data.headers.map(h=>`<div><dt>${escapeHtml(h)}</dt><dd>${performanceFormat(row.values[h])}</dd></div>`).join('')}</dl>`;
 detailPanel.classList.add('open');detailPanel.setAttribute('aria-hidden','false');detailPanel.querySelector('[data-close-detail].close-button')?.focus();
 loadPerformanceHistory(id,30);
}
let performanceHistoryPoints=[];
async function loadPerformanceHistory(id,days){const request=++detailRequest;for(const button of detailContent.querySelectorAll('[data-performance-days]')){button.classList.toggle('active',Number(button.dataset.performanceDays)===days);button.onclick=()=>loadPerformanceHistory(id,Number(button.dataset.performanceDays));}const target=document.getElementById('performanceHistory');target.innerHTML='<div class="loading-card">Loading sales and order history…</div>';try{const result=await api(`/api/dashboard/performance/history?storeId=${encodeURIComponent(id)}&days=${days}`);if(request!==detailRequest)return;performanceHistoryPoints=result.points.map(point=>({...point,recordedAt:point.reportDate+'T00:00:00Z'}));target.innerHTML=`<p class="chart-hint">Hover or tap a point for its exact date and value.</p><div class="chart-card"><h4>Successful orders</h4><canvas class="chart" id="performanceOrdersChart" aria-label="Successful orders history chart"></canvas></div><div class="chart-card"><h4>Gross sales · AED</h4><canvas class="chart" id="performanceSalesChart" aria-label="Gross sales history chart"></canvas></div><p class="updated">${performanceHistoryPoints.length} saved daily reports</p>`;requestAnimationFrame(redrawPerformanceHistory);}catch{if(request!==detailRequest)return;target.innerHTML='<section class="history-placeholder"><strong>Performance history unavailable</strong><p>Saved reports could not be loaded.</p></section>';}}
function redrawPerformanceHistory(){drawChart(document.getElementById('performanceOrdersChart'),performanceHistoryPoints,'orders','#56a5ff',{min:0,decimals:0,label:'Orders',dateOnly:true});drawChart(document.getElementById('performanceSalesChart'),performanceHistoryPoints,'grossSales','#36d399',{min:0,decimals:0,label:'Gross sales · AED',dateOnly:true});}
